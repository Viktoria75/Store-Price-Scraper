# Price Tracker - Следене на цени от онлайн магазини

Десктоп приложение за автоматично следене и събиране на информация за цени на продукти от онлайн магазини.

## 📋 Функционалност

- **Добавяне и управление на продукти** - URL адрес + CSS селектор / XPath
- **Автоматично събиране на данни** - периодична проверка на цените
- **Ръчно обновяване** - опресняване на цените по желание
- **Визуализация на данни** - таблица с продукти + графика на ценовата история
- **Известяване при промяна** - Email и Discord известия при спад на цена
- **Импорт/Експорт** - CSV и JSON формат
- **Drag-and-drop** - плъзгане на URL адреси директно в приложението

## 🚀 Инсталация

### Предварителни изисквания

- Python 3.10 или по-нова версия
- Google Chrome (за Selenium функционалност)

### Стъпки за инсталация

1. **Клонирайте хранилището:**
   ```bash
   git clone <repository-url>
   cd Project
   ```

2. **Създайте виртуална среда:**
   ```bash
   python -m venv venv
   venv\Scripts\activate  # Windows
   # или
   source venv/bin/activate  # Linux/macOS
   ```

3. **Инсталирайте зависимостите:**
   ```bash
   pip install -r requirements.txt
   ```

4. **За разработка (тестове, linting):**
   ```bash
   pip install -r requirements-dev.txt
   ```

## 📖 Използване

### Стартиране на приложението

```bash
cd src
python -m price_tracker.main
```

Или:

```bash
set PYTHONPATH=src
python -m price_tracker.main
```

### Добавяне на продукт

1. Кликнете **"➕ Добави"** в toolbar-а
2. Въведете URL адреса на продукта (напр. `https://www.ozone.bg/product/...`)
3. Въведете име на продукта или използвайте "Открий име автоматично"
4. Въведете CSS селектор за цената (напр. `.product-price`, `#price`)
5. Тествайте селектора с бутона "Тествай селектора"
6. Запазете

### Примерни CSS селектори

| Магазин | Селектор |
|---------|----------|
| ozone.bg | `.product-price .price` |
| emag.bg | `.product-new-price` |
| technopolis.bg | `.price-wrapper .price` |
| amazon.com | `#priceblock_ourprice` |

### Автоматично следене

1. Кликнете **"▶️ Старт"** за да активирате автоматичната проверка
2. Интервалът може да се конфигурира от **Редакция → Настройки**

## 🧪 Тестове

```bash
# Изпълняване на тестовете
set PYTHONPATH=src
pytest tests/ -v

# С покритие
pytest tests/ -v --cov=src/price_tracker --cov-report=html

# Проверка на типовете
mypy src/price_tracker

# Linting
pylint src/price_tracker
```

## 📁 Структура на проекта

```
Project/
├── src/price_tracker/
│   ├── models/          # Модели на данни
│   ├── scraper/         # Web scraping модули
│   ├── storage/         # Съхранение на данни
│   ├── notifications/   # Email/Discord известия
│   ├── scheduler/       # Фонова проверка на цени
│   └── gui/             # PyQt6 графичен интерфейс
├── tests/               # Тестове
├── data/                # Данни (JSON файлове)
├── requirements.txt     # Зависимости
└── pyproject.toml       # Конфигурация на проекта
```

## 📜 Лиценз

MIT License
